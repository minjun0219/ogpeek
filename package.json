{
  "name": "ogpeek",
  "private": true,
  "version": "0.2.0",
  "description": "peek into any page's Open Graph tags",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -F web build",
    "test": "pnpm -r --filter './packages/*' test",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm -F web dev"
  },
  "devDependencies": {
    "typescript": "5.9.2"
  }
}
